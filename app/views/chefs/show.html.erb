<h1>Chef: <%= @chef.name %></h1>
<br>
<h3><%= link_to "View All Ingredients Used by #{@chef.name}", "/ingredients/#{@chef.id}" %></h3>
<h4>"Most Popular ingredients:"</h4>
<ul>
  <% @chef.most_popular_ingredients.each do |ingredient| %>
  <li><%= ingredient %></li>
  <% end %>
</ul>
